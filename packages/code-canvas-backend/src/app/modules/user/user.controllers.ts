import { Request, Response } from "express";
import catchAsync from "../../../shared/utils/catchAsync";

export const getAllUsers = catchAsync((req:Request, res:Response) => {
 
})

export const getUserById = catchAsync((req:Request, res:Response) => {
 
})

export const updateUserById = catchAsync((req:Request, res:Response) => {
 
})

export const deleteUserById = catchAsync((req:Request, res:Response) => {
 
})